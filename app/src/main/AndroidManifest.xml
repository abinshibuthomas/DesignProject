<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.abinshibuthomas.designproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".home">
        </activity>
        <activity android:name=".smartlibrary">
        </activity>
        <activity
            android:name=".searchbook"
            android:label="@string/title_activity_searchbook"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".availablebook">
        </activity>
        <activity android:name=".unavailable">
        </activity>
        <activity android:name=".faculty">
        </activity>
        <activity android:name=".expoAbin"/>
    </application>

</manifest>